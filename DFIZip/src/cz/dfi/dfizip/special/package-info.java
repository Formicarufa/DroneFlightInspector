/**
 * Creating of quantities with a special meaning.
 * This package contains important interfaces {@link SpecialDoubleQuantProvider}
 * and {@link SpecialGroupProvider} that  can be implemented and registered
 * as services in the global lookup using the ServiceProvider annotation,
 * if the user wants to assign to a certain quantity name a special wrapper class.
 */
package cz.dfi.dfizip.special;
