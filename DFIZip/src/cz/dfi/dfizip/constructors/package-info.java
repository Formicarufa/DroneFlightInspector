/**
 * Constructors actually generate the series, when all data are parsed.
 * The {@link providers.ConstructorProvider}s create constructors,
 * which store the data about the series they are about to construct.
 * 
 */
package cz.dfi.dfizip.constructors;
