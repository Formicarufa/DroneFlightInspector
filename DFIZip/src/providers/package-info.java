/**
 * Creating of a specific data type series with potentially specific CSV deserialization code.
 * To be able to read an element with a new name from the XML description,
 * read the values in the CSV file and insert them into a specific Values Wrapper,
 * implement the {@link ConstructorProvider} interface and register it 
 * as a service in the global lookup using the ServiceProvider annotation.
 */
package providers;
